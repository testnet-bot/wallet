export async function detectDustTokens(walletAddress: string) {
  // Stub: replace with actual token scan logic
  return [
    { symbol: 'DUST1', balance: 0.01 },
    { symbol: 'DUST2', balance: 0.05 },
  ]
}
